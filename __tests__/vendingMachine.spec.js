const vendingMachine = require("../lib/vendingMachine");

describe("vendingMachine", () => {
  describe("", () => {
    it("", () => {
      const vendingMachine1 = new vendingMachine(
        ["Coke", "Pepsi"],
        ["25", "5"]
      );
      console.log(vendingMachine1.productsInventory);
      expect().toEqual();
    });
  });
});
